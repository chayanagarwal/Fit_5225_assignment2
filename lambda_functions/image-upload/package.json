{
	"name": "image-upload",
	"version": "1.0.0",
	"description": "Lambda function to upload images top S3 bucket",
	"main": "handler.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"pub": "serverless deploy function -f upload",
		"dep": "serverless deploy"
	},
	"keywords": [
		"AWS",
		"Lambda",
		"NodeJS"
	],
	"author": "Kshitij Pandey",
	"license": "ISC",
	"dependencies": {
		"aws-sdk": "^2.686.0",
		"file-type": "^14.5.0"
	}
}
