{
	"name": "query-tags",
	"version": "1.0.0",
	"description": "A Lambda function to query tags stored in DynamoDB",
	"main": "handler.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"pub": "serverless deploy function -f query",
		"dep": "serverless deploy"
	},
	"author": "Kshitij Pandey",
	"license": "ISC",
	"dependencies": {
		"aws-sdk": "^2.688.0"
	}
}
